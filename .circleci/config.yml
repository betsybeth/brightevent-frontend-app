version: 2.1
jobs: 
  build:
     working_directory: ~/brightevent-frontend-app
     steps: 
      - checkout
      - run:
          name: install dependencies
          command: yarn install
      - run: 
          name: run tests
          command: yarn test
workflows:
  version: 2
  commit:
     jobs:
      - build 